<!DOCTYPE html>
<html lang="pt">

<head>
    <meta charset="UTF-8">
    <title>Parts.COM - Sistema de Vendas</title>
    <style>
      /* ====== Estrutura base ====== */
body {
    font-family: 'Segoe UI', Arial, sans-serif;
    margin: 0;
    background-color: #f3f3f3;
}

.container {
    max-width: 1200px;
    margin: 20px auto;
    background-color: #fff;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
    border-radius: 10px;
    overflow: hidden;
}

/* ====== Cabeçalho ====== */
.header {
    background: linear-gradient(135deg, #fb0303ff, #1b1919ff);
    color: white;
    padding: 20px;
    text-align: center;
}
.header h1 {
    margin: 0;
    letter-spacing: 1px;
}

/* ====== Navegação ====== */
.navigation {
    background-color: #f60909ff;
    display: flex;
    justify-content: space-evenly;
    flex-wrap: wrap;
}
.navigation a {
    flex-grow: 1;
    color: white;
    text-align: center;
    padding: 15px 0;
    text-decoration: none;
    font-weight: bold;
    transition: background-color 0.3s, transform 0.2s;
}
.navigation a:hover {
    background-color: #0a0a0aff;
    transform: scale(1.03);
}
.navigation a.active {
    background-color: #ffffffff;
    pointer-events: none;
}
.navigation a.logout-btn {
    margin-left: auto;
}

/* ====== Conteúdo ====== */
.content {
    padding: 30px;
    min-height: 400px;
}
.content h2 {
    margin-top: 0;
    color: #1d1d1d;
}

/* ====== Tabela ====== */
.styled-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 1.5em;
    font-size: 0.95rem;
}
.styled-table th,
.styled-table td {
    border: 1px solid #ddd;
    padding: 10px 12px;
    text-align: left;
}
.styled-table th {
    background-color: #250385;
    color: white;
}
.styled-table tr:nth-child(even) {
    background-color: #f8f8f8;
}
.styled-table tr:hover {
    background-color: #e0e0ff;
}

/* ====== Botões ====== */
.button-action {
    display: inline-block;
    padding: 10px 18px;
    background-color: #2bee59;
    color: white !important;
    border-radius: 5px;
    border: none;
    cursor: pointer;
    font-weight: bold;
    transition: 0.2s;
}
.button-action:hover {
    background-color: #22c74d;
    transform: scale(1.05);
}

/* ====== Login ====== */
.login-box {
    max-width: 400px;
    margin: 80px auto;
    text-align: center;
}
.form-container {
    background-color: #272727;
    padding: 40px;
    border-radius: 10px;
    box-shadow: 0 5px 25px rgba(0, 0, 0, 0.3);
}
.login-box h2,
.login-box label,
.login-box .error-message {
    color: white;
}
.login-box input {
    width: 85%;
    padding: 10px;
    margin: 10px 0;
    border: none;
    border-radius: 5px;
    background: rgba(255, 255, 255, 0.15);
    color: white;
    font-size: 1rem;
}
.login-box input::placeholder {
    color: rgba(255, 255, 255, 0.8);
}
.login-box button {
    background-color: #2bee59;
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    margin-top: 20px;
    transition: 0.2s;
}
.login-box button:hover {
    background-color: #22c74d;
    transform: scale(1.05);
}

/* ====== Outras ====== */
.page-title {
    margin-top: 0;
    margin-bottom: 20px;
}
.venda-header p {
    font-size: 1.1em;
    margin: 5px 0;
}
.venda-itens h3 {
    margin-top: 30px;
}
.input-quantidade {
    width: 60px;
    padding: 5px;
    box-sizing: border-box;
}

    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>Autopeças</h1>
        </div>

        <div class="navigation">
            <?php
            $currentController = isset($controllerName) ? $controllerName : '';
            $currentAction = isset($actionName) ? $actionName : '';
            ?>
            <a href="/index.php?controller=produto&action=index"
                class="<?php echo ($currentController === 'produto') ? 'active' : ''; ?>">Adicionar Produtos</a>

            <a href="/index.php?controller=venda&action=index"
                class="<?php echo ($currentController === 'venda' && $currentAction === 'index') ? 'active' : ''; ?>">Vender Produtos</a>

            <a href="/index.php?controller=venda&action=listar"
                class="<?php echo ($currentController === 'venda' && $currentAction === 'listar') ? 'active' : ''; ?>">Relatório de Vendas</a>

            <?php if (isset($_SESSION['user_id'])): ?>
                <a href="/index.php?controller=user&action=logout" class="logout-btn">Sair</a>
            <?php endif; ?>
        </div>

        <div class="content">
            <?php require_once $content_view; ?>
        </div>
    </div>
</body>

</html>