<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>Parts.COM - Sistema de Vendas</title>
    <style>
    body { font-family: Arial, sans-serif; margin: 0; background-color: #f0f0f0; }
    .container { max-width: 1000px; margin: 20px auto; background-color: #fff; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
    .header { background-color: #0c0841ff; color: white; padding: 20px; text-align: center; }
    .header h1 { margin: 0; }
    .navigation { background-color: #444444ff; display: flex; justify-content: center; padding: 10px 0; gap: 20px; }
    .navigation a { color: white; text-align: center; text-decoration: none; font-weight: bold; }
    .navigation a:hover { text-decoration: underline; }
    .content { padding: 20px; min-height: 400px; }
    .content h2 { margin-top: 0; }
    .content a.link-action { display: inline-block; margin-bottom: 20px; }
    .content table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    .content th, .content td { border: 1px solid #ccc; padding: 8px; text-align: left; }
    .content th { background-color: #f2f2f2; }
    .login-box { max-width: 400px; margin: 50px auto; text-align: center; }
    .login-box .form-container { background-color: #414141ff; padding: 30px; border-radius: 10px; }
    .login-box input { width: 80%; padding: 10px; margin: 10px 0; border: none; border-radius: 5px; }
    .login-box button { background-color: #2bee59ff; color: white; padding: 10px 20px; border: none; border-radius: 5px; cursor: pointer; }
    .content a { text-decoration: none;}

    .button-action {
        display: inline-block;
        padding: 10px 15px;
        background-color: #28a745;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        border: none;
        cursor: pointer;
        font-weight: bold;
    }
    .navigation { 
        background-color: #250385ff; 
        display: flex; 
        justify-content: space-around; 
        padding: 0; 
    }
    .navigation a { 
        flex: 1; /* Isto faz com que o espaço seja dividido igualmente */
        color: white; 
        text-align: center; 
        padding: 14px 0; /* Ajuste para preencher a altura */
        text-decoration: none; 
        font-weight: bold;
        transition: background-color 0.3s;
    }
    .navigation a:hover { 
        background-color: #555; 
    }
    .navigation a.active {
        background-color: #333; /* Cor mais escura para o item ativo */
        pointer-events: none; /* Desativa o clique no link ativo */
    }
    .content a:link, .content a:visited {
        color: #1d1d1dff;
        text-decoration: none;
    }
    .button-action {
        display: inline-block;
        padding: 10px 15px;
        background-color: #28a745;
        color: white !important;
        text-decoration: none;
        border-radius: 5px;
        border: none;
        cursor: pointer;
        font-weight: bold;
    }
    
</style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Parts.COM</h1>
        </div>

        <div class="navigation" text->
            <a href="/index.php?controller=produto&action=index">Adicionar Produtos</a>
            <a href="/index.php?controller=venda&action=index">Vender Produtos</a>
            <a href="/index.php?controller=venda&action=listar">Relatório de Vendas</a>
        </div>

        <div class="content">
            <?php require_once $content_view; ?>
        </div>
    </div>
</body>
</html>