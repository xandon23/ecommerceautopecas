<form action="/index.php?controller=venda&action=criar" method="POST">
    <table class="styled-table">
        <thead>
            <tr>
                <th>Produto</th>
                <th>Valor de Venda</th>
                <th>Quantidade</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($produtos as $produto): ?>
            <tr>
                <td><?php echo htmlspecialchars($produto->getName()); ?></td>
                <td>R$ <?php echo htmlspecialchars(number_format($produto->getValorvenda(), 2, ',', '.')); ?></td>
                <td>
                    <input type="number"
                           name="produtos[<?php echo $produto->getId(); ?>]"
                           value="0"
                           min="0"
                           class="input-quantidade">
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>

    <button type="submit" class="button-action" style="margin-top: 20px;">Finalizar Venda</button>
</form>