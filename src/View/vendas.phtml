<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>Nova Venda - Sistema de Vendas</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 2em; }
        form { background: #f9f9f9; padding: 2em; border: 1px solid #ccc; border-radius: 8px; max-width: 600px; }
        ul { list-style: none; padding: 0; }
        li { margin-bottom: 1em; padding-bottom: 1em; border-bottom: 1px solid #eee; }
        input[type="number"] { width: 60px; padding: 5px; }
        button { margin-top: 1.5em; padding: 10px 15px; background-color: #28a745; color: white; border: none; border-radius: 5px; cursor: pointer; }
        button:hover { background-color: #218838; }
    </style>
</head>
<body>
    <h2>Nova Venda</h2>
    <form action="/index.php?controller=venda&action=criar" method="POST">
        <ul>
            <?php foreach ($produtos as $produto): ?>
                <li>
                    <span><?php echo htmlspecialchars($produto->getName()); ?></span>
                    <br>
                    <small>Valor: R$ <?php echo htmlspecialchars(number_format($produto->getValorvenda(), 2, ',', '.')); ?></small>
                    <br>
                    <label for="quantidade-<?php echo $produto->getId(); ?>">Quantidade:</label>
                    <input type="number" 
                           name="produtos[<?php echo $produto->getId(); ?>]" 
                           id="quantidade-<?php echo $produto->getId(); ?>" 
                           value="0" 
                           min="0">
                </li>
            <?php endforeach; ?>
        </ul>
        <button type="submit">Finalizar Venda</button>
    </form>
</body>
</html>